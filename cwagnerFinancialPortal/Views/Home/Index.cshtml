@using cwagnerFinancialPortal.Extensions

@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <div class="col-md-6">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title"><a href="@Url.Action("Index", "BankAccount")">Click here to enter: @User.Identity.GetHouseholdName()</a></h4>
            </div>
        </div>
    </div>
</div>